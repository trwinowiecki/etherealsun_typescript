/* eslint-disable import/no-cycle */
import base from './base';
import { extend } from './utils';

export default extend(base, {
  primary: '#61DAFB',
  primaryDarker: '#C19728',
  secondary: '#376B40',
  secondaryDarker: '#62BD72',
  textPrimary: '#ddd',
  backgroundPrimary: '#F0E0C5',
  backgroundPrimaryDarker: '#FAF1E2'
});
